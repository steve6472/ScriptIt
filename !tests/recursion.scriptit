import library System;

// This dies probably 'cause variables are "the same" when stack is pushed
function fibonacci(int num)
{
    if (num == 0) { return 0; };
    if (num == 1) { return 1; };
    num = num - 1;

    return fibonacci(num);
};

return fibonacci(3);
