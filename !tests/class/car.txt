import library System;
import type int;
import type string;

class Car
{
    string name;
    int wheelCount = 3;
};

Car car = Car();

§car.name = "Subuwu";
§car.wheelCount = 4;

System.printDetail(car);