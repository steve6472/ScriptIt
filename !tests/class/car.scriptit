import library System;
import type int;
import type string;

class Car
{
    string name;
    int wheelCount = 3;

    function printName()
    {
        System.dumpVariables();
        System.println("Car Name: '" + name + "'");
    };
};

Car car = Car();

car.name = "Subuwu";
car.wheelCount = 4;

System.printDetail(car);
car.printName();

return car;