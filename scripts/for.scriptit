import int;
import double;
import string;
import bool;
import char;
import vec2;
import array;

import functions log;

for (int iter = 0, iter < 5, iter = iter + 1)
{
    function void t()
    {
        iter.print();
        // TODO: make this work
        //break;
    }

    t();
}